// Se não houver modo escuro, o botão não precisa fazer nada
const btn = document.getElementById("themeToggle");

// Opcional: desativar o botão
if (btn) {
  btn.style.display = "none"; // esconde o botão
}
